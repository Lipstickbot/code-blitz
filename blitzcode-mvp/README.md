# BlitzCode MVP

This folder now contains the backend workspace for the Code Blitz MVP.

The active frontend is the static app in the repository root:

```text
../index.html
../app.js
../styles.css
../config.js
```

## Backend

Backend files live in:

```text
backend/
```

See `backend/README.md` for local development, migrations, seed data and staging deployment.
